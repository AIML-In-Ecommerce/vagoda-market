export type AddressType = {
    _id: string;
    receiverName: string,
    address: string,
    phoneNumber: string,
    addressType: string,
    selectedAsDefault: boolean // is selected as default address
}